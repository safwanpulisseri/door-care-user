import 'package:flutter/material.dart';

class FindLocationBookService extends StatelessWidget {
  FindLocationBookService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Loaction'),
      ),
    );
  }
}
