import 'package:flutter/material.dart';

class CompletedBookService extends StatelessWidget {
  CompletedBookService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Comepleted'),
      ),
    );
  }
}
